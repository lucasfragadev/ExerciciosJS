// Arquivo exercicio013.js

/*

function calcImc (peso, altura) {
  const imc = (peso / parseFloat(altura ** 2))

  if (imc < 18.5) {
    console.log(`IMC: ${imc.toFixed(2)} // Abaixo do peso.`)
  } else if (imc >= 18.5 && imc < 25) {
    console.log(`IMC: ${imc.toFixed(2)} // Normal.`)
  } else if (imc >= 25 && imc < 30) {
    console.log(`IMC: ${imc.toFixed(2)} // Sobrepeso // Obesidade grau I`)
  } else if (imc >= 30 && imc <= 39.9) {
    console.log(`IMC: ${imc.toFixed(2)} // Obesidade // Obesidade grau II`)
  } else {
    console.log(`IMC: ${imc.toFixed(2)} // Obesidade grave // Obesidade grau III`)
  }
}

calcImc(110, 1.71)

*/